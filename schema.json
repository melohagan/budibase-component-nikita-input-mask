{
  "type": "component",
  "metadata": {},
  "schema": {
    "name": "nikita-input-mask",
    "friendlyName": "nikita-input-mask",
    "description": "Mask inputs for credit cards, phone numbers, etc",
    "icon": "Text",
    "settings": [
      {
        "type": "field/string",
        "label": "Field",
        "key": "field",
        "required": true
      },
      {
        "type": "text",
        "label": "Label",
        "key": "label"
      },
      {
        "type": "text",
        "key": "mask",
        "label": "Mask",
        "defaultValue": "0000-000000-00000",
        "required": true
      }
    ]
  }
}
